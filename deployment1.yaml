apiVersion: v1
kind: Service
metadata:
  name: a1-python
spec:
  selector:
    app: a1-python
  ports:
  - name: http
    protocol: TCP
    port: 6001
    targetPort: 5000
  type: LoadBalancer


---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: a1-python
  
spec:
  replicas: 2
  selector:
    matchLabels:
      app: a1-python
  template:
    metadata:
      labels:
        app: a1-python
    spec:
      containers:
      - name: a1-python
        image: phanick/a1-python:1
        imagePullPolicy: Always
        ports:
        - containerPort: 5000
