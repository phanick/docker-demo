apiVersion: v1
kind: Service
metadata:
  name: a4-python
spec:
  selector:
    app: a4-python
  ports:
  - name: http
    protocol: TCP
    port: 6001
    targetPort: 5000
  type: LoadBalancer


---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: a4-python
  
spec:
  replicas: 2
  selector:
    matchLabels:
      app: a4-python
  template:
    metadata:
      labels:
        app: a4-python
    spec:
      containers:
      - name: a4-python
        image: phanick/a4-python:4
        imagePullPolicy: Always
        ports:
        - containerPort: 5000
