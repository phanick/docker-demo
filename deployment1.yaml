apiVersion: v1
kind: Service
metadata:
  app: a5-python
  name: a5-python
spec:
  selector:
    app: a5-python
  ports:
  - name: http
    protocol: TCP
    port: 6001
    targetPort: 5000
  type: LoadBalancer


---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: a5-python
  
spec:
  replicas: 2
  selector:
    matchLabels:
      app: a5-python
  template:
    metadata:
      labels:
        app: a5-python
    spec:
      containers:
      - name: a5-python
        image: phanick/a5-python:5
        imagePullPolicy: Always
        ports:
        - containerPort: 5000
