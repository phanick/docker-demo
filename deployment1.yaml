apiVersion: v1
kind: Service
metadata:
  name: a6-python
spec:
  selector:
    app: a6-python
  ports:
  - name: http
    protocol: TCP
    port: 6001
    targetPort: 5000
  type: LoadBalancer


---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: a6-python
  
spec:
  replicas: 2
  selector:
    matchLabels:
      app: a6-python
  template:
    metadata:
      labels:
        app: a6-python
    spec:
      containers:
      - name: a6-python
        image: phanick/a6-python:6  
        imagePullPolicy: Always
        ports:
        - containerPort: 5000
